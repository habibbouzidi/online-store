/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com", "sportstiger-images.s3.ap-south-1.amazonaws.com"]
    }
}

module.exports = nextConfig
